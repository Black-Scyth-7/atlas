# Deps for the isolated GPU training env (.venv-train) — see setup_train.py.
# torch + torchaudio are installed SEPARATELY from the CUDA wheel index (so the
# RTX 5050 / Blackwell sm_120 is used); everything else is listed here.
openwakeword
soundfile
torchinfo
scipy
tqdm
numpy
onnx
onnxscript      # torch.onnx.export needs this in torch 2.x (else export crashes)
# openWakeWord.data/.train training extras (not auto-installed)
pronouncing
audiomentations
torch-audiomentations
speechbrain
mutagen
acoustics
torchmetrics
