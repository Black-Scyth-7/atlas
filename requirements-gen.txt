# Deps for generating wake-word positive clips with piper-sample-generator.
# torch/torchaudio are installed separately by setup_gen.py from the CUDA (cu128)
# index so generation runs on the GPU. Kept out of the main .venv.
piper-tts==1.3.0
audiomentations==0.33.0
webrtcvad
numpy>=2,<3
